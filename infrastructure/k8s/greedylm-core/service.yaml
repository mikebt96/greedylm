apiVersion: v1
kind: Service
metadata:
  name: greedylm-core
  namespace: greedylm
spec:
  selector:
    app: greedylm-core
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
